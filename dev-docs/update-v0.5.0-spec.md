Add Emailing support
- Collect a new Member's Email Address (optional), and add a button to automatically send a 'Welcome' email to that address.

Add RestDB.io Database support 
- include a method of storing API secret (and database details) for local development running.
- Output and save a markdown document that details the database to be created in restdb.io
- I will create this database and the local credentials file so that we can run locally, but test that the database io works as designed.

(v0.6.0 will be preparing for remote hosting and production deploy)
