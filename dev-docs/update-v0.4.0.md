Put an 'Add a Bill' button to the dashboard - take directly to the 'Log New Bill' dialog.
When adding new members to a group with 'require bills to have date ranges' NOT checked - default the 'Join Date' to Today (just like is already done for the 'Organizer')
Can we add a 'refresh' button to both the Dashboard and the 'My Bills' screens - just to refresh the data and redraw the balances/bills

Can we remove the 'Hi, {User}' and group name from the dashboard.
I would like the User Name and Group name to be in the header pane (perminently), something like "{User} - Group: {GroupName}"

The CSV export is including an eroneous character every time there is a currency symbol: 'Â£-38.33' - but only when the csv is auto opening in excel (which will happen for most people) - i can see the raw ascii in the csv does not contain this 'Â' character. Can the encoding of the csv change slightly to aleviate this?.
Also the 'Payer' column of the 'involved bills' breakdown just says 'Unknown' on all bills - this should say the name of the bill payer.
The list of bills in the csv is showing the 'Your Share' as zero for all bills - not correctly calculating the share.
This list of bills is also showing the 'total Bill' amount - this should not be in the export.

